[project]
name = "houston-protocol"
version = "0.1.0"
description = "Shared protocol models for Houston edge and ground services"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.11.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
