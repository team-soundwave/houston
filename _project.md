# Houston

Python project with development scripts, tests, and package metadata. The root README should be used to clarify the product, but it appears to be an app/tool rather than a reference checkout.
